import 'package:flutter/material.dart';

class AlarmSpace extends StatelessWidget{
  const AlarmSpace({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      backgroundColor: Colors.white,
      body: Container(

      ),
    );
  }
}