import 'package:flutter/material.dart';

class SaveSpace extends StatelessWidget{
  const SaveSpace({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      backgroundColor: Colors.white,
      body: Container(

      ),
    );
  }
}